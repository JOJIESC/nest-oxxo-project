version: '3'

services:
  db:
    image: postgres:14.3
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_DB: oxxo_db
    container_name: oxxo_db
    volumes:
      - ./postgres:/var/lib/postgresql/data
    